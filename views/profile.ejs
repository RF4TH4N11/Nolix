<!DOCTYPE html>
<html lang="en">
<%- include('partials/headerDashboard') %>

<body class="bg-gray-900 text-white min-h-screen flex flex-col">
    <main class="flex-1 p-8">
        <div class="max-w-xl mx-auto bg-gray-800 p-6 rounded-lg shadow-lg">
            <h2 class="text-2xl font-bold mb-4 text-center">My Profile</h2>
            <form action="/profiles" method="POST" class="space-y-4">
                <div>
                    <label class="block mb-1 text-sm">Username</label>
                    <input type="text" value="<%= user.userName %>" disabled
                        class="w-full p-2 rounded bg-gray-700 border border-gray-600 text-gray-400">
                </div>

                <div>
                    <label class="block mb-1 text-sm">First Name</label>
                    <input type="text" name="firstName" value="<%= profile?.firstName || '' %>"
                        class="w-full p-2 rounded bg-gray-700 border border-gray-600" required>
                </div>

                <div>
                    <label class="block mb-1 text-sm">Last Name</label>
                    <input type="text" name="lastName" value="<%= profile?.lastName || '' %>"
                        class="w-full p-2 rounded bg-gray-700 border border-gray-600" required>
                </div>

                <div>
                    <label class="block mb-1 text-sm">Phone Number</label>
                    <input type="string" name="phoneNumber" value="<%= profile?.phoneNumber || '' %>"
                        class="w-full p-2 rounded bg-gray-700 border border-gray-600" required>
                </div>

                <button type="submit"
                    class="w-full bg-red-600 hover:bg-red-700 py-2 rounded font-semibold">Save Profile</button>
            </form>
        </div>
    </main>

    <%- include('partials/footer') %>
</body>
</html>
